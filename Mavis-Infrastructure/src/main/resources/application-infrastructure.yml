aws:
  region: ap-northeast-2

spring:
  mail:
    host: smtp.naver.com
    port: 587
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.debug: true

logging:
  level:
    feign: DEBUG

---

spring:
  config:
    activate:
      on-profile: local
discord:
  webhook:
    url: http://localhost
aws:
  s3:
    access-key: local
    secret-key: local
    bucket-name: local
