# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Discord 알림

Discord 전송 필요 시 `/discord` 커맨드 사용. URL은 `.claude/settings.local.json`의 `env.DISCORD_WEBHOOK`에 저장 (gitignored).
사용자가 명시적으로 "Discord에 보내줘" 요청 시 전송한다.
